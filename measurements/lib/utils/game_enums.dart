enum MeasurementSystem {
  metric,
  imperial,
  universal,
}

enum AgeGroup {
  earlyLearners,
  foundationLearners,
  advancedLearners,
}

enum Topic {
  length,
  weight,
  volume,
  temperature,
}
